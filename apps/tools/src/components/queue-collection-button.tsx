"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function QueueCollectionButton() { const router = useRouter(); const [message, setMessage] = useState(""); const [pending, setPending] = useState(false); async function queue() { setPending(true); setMessage(""); const response = await fetch("/api/jobs", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ type: "collection" }) }); const body = await response.json(); setPending(false); setMessage(response.ok ? "Collection queued. The existing worker will pick it up." : body.error ?? "Could not queue collection."); if (response.ok) router.refresh(); } return <div><button className="primary compact" disabled={pending} onClick={queue}>{pending ? "Queueing…" : "Queue collection"}</button>{message && <p className={message.startsWith("Collection") ? "data-note" : "error"}>{message}</p>}</div>; }
