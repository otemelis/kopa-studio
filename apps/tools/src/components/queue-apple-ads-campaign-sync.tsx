"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function QueueAppleAdsCampaignSync({ connectionId }: { connectionId: string }) { const [message, setMessage] = useState(""); const [pending, setPending] = useState(false); const router = useRouter(); async function call(url: string, body?: object) { setPending(true); const response = await fetch(url, { method: "POST", headers: body ? { "Content-Type": "application/json" } : undefined, body: body ? JSON.stringify(body) : undefined }); const result = await response.json(); setPending(false); setMessage(response.ok ? (result.status === "completed" ? `Imported ${result.campaigns} campaign(s).` : "Campaign sync queued.") : result.error ?? "Could not run sync."); if (response.ok) router.refresh(); } return <div><button className="primary compact" disabled={pending} onClick={() => call("/api/apple-ads/sync/campaigns", { connectionId })}>{pending ? "Working…" : "Queue campaign sync"}</button><button className="secondary compact" disabled={pending} onClick={() => call("/api/apple-ads/sync/run-owner")}>Run queued sync now</button>{message ? <p className={message.startsWith("Imported") || message.startsWith("Campaign") ? "data-note" : "error"}>{message}</p> : null}</div>; }
