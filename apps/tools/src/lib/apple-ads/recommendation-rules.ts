import type { EvidenceStage } from "./low-volume-model";
export type RecommendationType="ADD_EXACT_KEYWORD"|"ADD_NEGATIVE_EXACT"|"INCREASE_KEYWORD_BID"|"DECREASE_KEYWORD_BID"|"KEEP_LEARNING"|"INSUFFICIENT_EVIDENCE";
export type RuleInput={relevance:number;confidence:number;financialRisk:number;evidence:EvidenceStage;isControlledExact:boolean;isNegative:boolean;spend:number;learningCap:number;fresh:boolean;partialSync:boolean;protectedTerm:boolean;recentBidChange:boolean};
export function recommend(input:RuleInput):RecommendationType { if(!input.fresh||input.partialSync)return "INSUFFICIENT_EVIDENCE"; if(input.relevance<25&&!input.protectedTerm&&input.spend>0&&input.confidence>=60)return "ADD_NEGATIVE_EXACT"; if(!input.isControlledExact&&!input.isNegative&&input.relevance>=75&&input.evidence!=="NO_EVIDENCE"&&input.evidence!=="MARKET_SIGNAL_ONLY")return "ADD_EXACT_KEYWORD"; if(input.spend>=input.learningCap&&input.financialRisk>=100)return "DECREASE_KEYWORD_BID"; if(input.relevance>=80&&input.confidence>=70&&input.financialRisk<50&&!input.recentBidChange&&input.evidence==="REPEATED_EVIDENCE")return "INCREASE_KEYWORD_BID"; return input.evidence==="NO_EVIDENCE"?"INSUFFICIENT_EVIDENCE":"KEEP_LEARNING"; }
