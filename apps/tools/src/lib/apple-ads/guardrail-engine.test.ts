import {expect,it} from "vitest";import {evaluateGuardrails} from "./guardrail-engine";const base={fresh:true,partialSync:false,emergencyPaused:false,currencyKnown:true,currentBid:1,proposedBid:1,maxBid:2,lastChangeAt:null,now:"2026-07-21T12:00:00Z",minimumHoursBetweenChanges:48,dailySpend:1,dailyHardCap:10};it("blocks stale and unsafe actions",()=>expect(evaluateGuardrails({...base,fresh:false,proposedBid:3}).reasons).toEqual(["Data is stale.","Proposed bid exceeds cap."]));
